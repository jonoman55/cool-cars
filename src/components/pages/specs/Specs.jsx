import { CardWrapper, CardText } from '../../shared';

/**
 * Specs
 */
const Specs = () => {
  return (
    <CardWrapper>
      <CardText text={"Specs"} />
    </CardWrapper>
  );
};

export default Specs;
