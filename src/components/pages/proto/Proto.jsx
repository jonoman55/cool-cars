import { CardWrapper, CardText } from '../../shared';

/**
 * Prototype
 */
const Proto = () => {
  return (
    <CardWrapper>
      <CardText text={"Prototype"} />
    </CardWrapper>
  );
};

export default Proto;
